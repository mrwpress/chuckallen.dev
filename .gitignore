# ── Dependencies ──
node_modules/

# ── Astro ──
dist/
.astro/

# ── Environment ──
.env
.env.*
!.env.example

# ── OS ──
.DS_Store
Thumbs.db

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo
*~

# ── Data (fetched at build time) ──
# src/data/reviews.json  ← temporarily tracked while GBP is suspended

# ── Build / Cache ──
.cache/
.turbo/
*.tsbuildinfo

# ── Logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Cloudflare ──
.wrangler/
.dev.vars

# ── NOTE: .claude/ is intentionally tracked ──
# We back up CLAUDE.md, settings, and project context in git.
